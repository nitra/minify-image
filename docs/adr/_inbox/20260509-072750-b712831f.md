---
session: b712831f-fb1f-46ba-8562-3313b3127de5
captured: 2026-05-09T07:27:50+03:00
transcript: /Users/vitaliytv/.claude/projects/-Users-vitaliytv-www-nitra-minify-image/b712831f-fb1f-46ba-8562-3313b3127de5.jsonl
---

## ADR Відповідність правилам `@nitra/cursor` у репозиторії `minify-image`

**Контекст:** Репозиторій `minify-image` не відповідав набору правил `@nitra/cursor` (9 правил). Запуск `npx @nitra/cursor check` виявив кілька порушень: відсутній `capture-decisions.sh`, відсутній `npm/jsconfig.json`, відсутній крок `depcheck` у GitHub Actions, відсутнє розширення `timonwong.shellcheck` у VS Code, відсутня команда `run-shellcheck-text.mjs` у `lint-text`, а також декілька невідомих слів у `.cspell.json`.

**Рішення/Процедура/Факт:**

1. Запущено `npx @nitra/cursor` для копіювання канонічних файлів — з'явився `.claude/hooks/capture-decisions.sh`; до `.gitignore` додано рядок `.claude/hooks/capture-decisions.log`.
2. Створено `npm/jsconfig.json` з канонічним вмістом (`NodeNext`, `include: src/**/*`).
3. До `.github/workflows/npm-publish.yml` додано крок `npx depcheck --ignores="graphql,bun"` з `working-directory: npm`.
4. До `.vscode/extensions.json` додано `timonwong.shellcheck`.
5. До `package.json` у скрипт `lint-text` додано `bun ./node_modules/@nitra/cursor/scripts/run-shellcheck-text.mjs`.
6. До `.cspell.json` додано слова `taze`, `кастомні`, `мініфікатор`, `мініфікація`, `мініфікувати`.
7. Оскільки `npm/` змінився (додано `jsconfig.json`), версію пакета підвищено `3.3.2` → `3.3.3` і додано секцію `## [3.3.3] - 2026-05-09` до `npm/CHANGELOG.md`.
8. Запущено `lint-js`, `lint-text`, `lint-ga` — всі завершились із `0` помилок. Фінальний `npx @nitra/cursor check` — 9/9 правил успішно.

**Обґрунтування:** Правило `n-fix` вимагає відповідності проєкту всім активним правилам `@nitra/cursor`. Кожен пункт виправлення відповідає конкретному правилу: `n-adr` → `capture-decisions.sh`; `n-js-run` → `jsconfig.json` і `depcheck`; `n-text` → `shellcheck` у VS Code і `run-shellcheck-text.mjs` у `lint-text`; `n-npm-module` + `n-changelog` → бамп версії і запис у CHANGELOG при зміні `npm/`.

**Розглянуті альтернативи:** Не обговорювалися — скіл `n-fix` передбачає суворе дотримання правил без відхилень.

**Зачіпає:**

- `.claude/hooks/capture-decisions.sh` (новий файл)
- `.gitignore`
- `npm/jsconfig.json` (новий файл)
- `.github/workflows/npm-publish.yml`
- `.vscode/extensions.json`
- `package.json` (корінь)
- `.cspell.json`
- `npm/package.json`
- `npm/CHANGELOG.md`
